<section>
    <h2>{{title}}</h2>
    <p>I'm currently trying to consolidate all of my little ideas and toy projects to this page (why bother with a website if
        I have nothing to show), the source for most of these is available on my GitHub.</p>
</section>
<section class='boxlist'>
    {{#each res.data}} {{#if this.published}}
    <div class='ideasbox' data-url='{{this.content.url}}' {{!--{{#if this.content.image}} style="
        background-image:url('{{this.content.image.thumbnails.med}}')
    " {{/if}}--}}>
        <header>
            <h1><a href='{{this.content.url}}'>{{this.title}}</a></h1>
        </header>
        <main>{{{this.content.description.html}}}</main>
    </div>
    {{/if}} {{/each}}
</section>